{
    "name": "api-payments-history",
    "version": "1.0.0",
    "description": "Full-stack payments history application with Express API and React frontend",
    "private": true,
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
        "dev:backend": "cd backend && npm run dev",
        "dev:frontend": "cd frontend && PORT=3001 npm start",
        "build": "npm run build:backend && npm run build:frontend",
        "build:backend": "cd backend && npm install",
        "build:frontend": "cd frontend && npm run build",
        "deploy": "npm run build:frontend && cd backend && npm run deploy",
        "deploy:full": "npm run build && npm run deploy",
        "bootstrap": "cd backend && npm run bootstrap",
        "test": "npm run test:backend && npm run test:frontend",
        "test:backend": "cd backend && npm test",
        "test:frontend": "cd frontend && npm test -- --watchAll=false"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": "22"
    }
}